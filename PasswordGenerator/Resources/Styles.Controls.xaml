<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- Single source of truth for the "controls display text in
       PrimaryTextBrush" rule. Button/CheckBox/RadioButton/TextBox each
       derive from this via BasedOn rather than repeating the Setter,
       since relying on Window-level Foreground inheritance to reach
       these controls did not work reliably in practice. -->
  <Style x:Key="BaseControlStyle" TargetType="Control">
    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
  </Style>

  <!-- ═══ BUTTON ═══ -->
  <Style TargetType="Button" BasedOn="{StaticResource BaseControlStyle}">
    <Setter Property="Padding" Value="0,5"/>
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="border"
                            BorderBrush="{DynamicResource ControlBorderBrush}"
                            BorderThickness="1"
                            Background="{DynamicResource SurfaceBrush}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          TextBlock.Foreground="{TemplateBinding Foreground}"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="border" Property="Background"
                                    Value="{DynamicResource ControlHoverBrush}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="border" Property="Background"
                                    Value="{DynamicResource ControlPressedBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="border" Property="Background"
                                    Value="{DynamicResource BackgroundGray}"/>
              <Setter TargetName="border" Property="BorderBrush"
                                    Value="{DynamicResource ControlDisabledBrush}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ═══ CHECKBOX ═══ -->
  <Style TargetType="CheckBox" BasedOn="{StaticResource BaseControlStyle}">
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="CheckBox">
          <StackPanel Orientation="Horizontal">
            <Border x:Name="box"
                                Width="16" Height="16"
                                BorderBrush="{DynamicResource ControlBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="3"
                                Background="{DynamicResource SurfaceBrush}"
                                VerticalAlignment="Center">
              <Path x:Name="tick"
                                  Data="M 2.5 7.7 L 5.5 10.5 L 11.5 4.5"
                                  Stroke="{DynamicResource AccentBrush}"
                                  StrokeThickness="1.5"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  Visibility="Collapsed"/>
            </Border>
            <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"
                                          TextBlock.Foreground="{TemplateBinding Foreground}"/>
          </StackPanel>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="tick" Property="Visibility" Value="Visible"/>
              <Setter TargetName="box"  Property="BorderBrush"
                                    Value="{DynamicResource AccentBrush}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="box" Property="BorderBrush"
                                    Value="{DynamicResource AccentBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="box" Property="Background"
                                    Value="{DynamicResource BackgroundGray}"/>
              <Setter TargetName="box" Property="BorderBrush"
                                    Value="{DynamicResource ControlDisabledBrush}"/>
              <Setter TargetName="tick" Property="Stroke"
                                    Value="{DynamicResource ControlBorderBrush}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ═══ RADIO BUTTON ═══ -->
  <Style TargetType="RadioButton" BasedOn="{StaticResource BaseControlStyle}">
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="RadioButton">
          <StackPanel Orientation="Horizontal">
            <Grid Width="16" Height="16" VerticalAlignment="Center">
              <Ellipse x:Name="outer"
                                     Width="16" Height="16"
                                     Stroke="{DynamicResource ControlBorderBrush}"
                                     StrokeThickness="1"
                                     Fill="{DynamicResource SurfaceBrush}"/>
              <Ellipse x:Name="inner"
                                     Width="8" Height="8"
                                     Fill="{DynamicResource AccentBrush}"
                                     Visibility="Collapsed"/>
            </Grid>
            <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"
                                          TextBlock.Foreground="{TemplateBinding Foreground}"/>
          </StackPanel>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="inner" Property="Visibility" Value="Visible"/>
              <Setter TargetName="outer" Property="Stroke"
                                    Value="{DynamicResource AccentBrush}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="outer" Property="Stroke"
                                    Value="{DynamicResource AccentBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="outer" Property="Stroke"
                                    Value="{DynamicResource ControlDisabledBrush}"/>
              <Setter TargetName="inner" Property="Fill"
                                    Value="{DynamicResource ControlBorderBrush}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ═══ SLIDER ═══ -->
  <Style TargetType="Slider">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Slider">
          <Grid VerticalAlignment="Center">
            <Track x:Name="PART_Track">
              <Track.DecreaseRepeatButton>
                <RepeatButton Command="Slider.DecreaseLarge">
                  <RepeatButton.Template>
                    <ControlTemplate TargetType="RepeatButton">
                      <Border Height="4"
                                                    Background="{DynamicResource AccentBrush}"
                                                    CornerRadius="2"/>
                    </ControlTemplate>
                  </RepeatButton.Template>
                </RepeatButton>
              </Track.DecreaseRepeatButton>
              <Track.Thumb>
                <Thumb>
                  <Thumb.Template>
                    <ControlTemplate TargetType="Thumb">
                      <Ellipse x:Name="knob"
                                                     Width="14" Height="14"
                                                     Fill="{DynamicResource SurfaceBrush}"
                                                     Stroke="{DynamicResource AccentBrush}"
                                                     StrokeThickness="2"/>
                      <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                          <Setter TargetName="knob" Property="Stroke"
                                                          Value="{DynamicResource ControlDisabledBrush}"/>
                        </Trigger>
                      </ControlTemplate.Triggers>
                    </ControlTemplate>
                  </Thumb.Template>
                </Thumb>
              </Track.Thumb>
              <Track.IncreaseRepeatButton>
                <RepeatButton Command="Slider.IncreaseLarge">
                  <RepeatButton.Template>
                    <ControlTemplate TargetType="RepeatButton">
                      <Border Height="4"
                                                    Background="{DynamicResource ControlDisabledBrush}"
                                                    CornerRadius="2"/>
                    </ControlTemplate>
                  </RepeatButton.Template>
                </RepeatButton>
              </Track.IncreaseRepeatButton>
            </Track>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ═══ THRESHOLD SLIDER ═══ -->
  <Style x:Key="ThresholdSlider" TargetType="Slider">
    <Setter Property="IsSelectionRangeEnabled" Value="True"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Slider">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="20"/>
              <RowDefinition Height="16"/>
            </Grid.RowDefinitions>

            <!-- Track row -->
            <Grid Grid.Row="0" VerticalAlignment="Center">
              <Track x:Name="PART_Track">
                <Track.DecreaseRepeatButton>
                  <RepeatButton Command="Slider.DecreaseLarge">
                    <RepeatButton.Template>
                      <ControlTemplate TargetType="RepeatButton">
                        <Border Height="4"
                                                    Background="{DynamicResource AccentBrush}"
                                                    CornerRadius="2"/>
                      </ControlTemplate>
                    </RepeatButton.Template>
                  </RepeatButton>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                  <Thumb>
                    <Thumb.Template>
                      <ControlTemplate TargetType="Thumb">
                        <Ellipse Width="14" Height="14"
                                                     Fill="{DynamicResource SurfaceBrush}"
                                                     Stroke="{DynamicResource AccentBrush}"
                                                     StrokeThickness="2"/>
                      </ControlTemplate>
                    </Thumb.Template>
                  </Thumb>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                  <RepeatButton Command="Slider.IncreaseLarge">
                    <RepeatButton.Template>
                      <ControlTemplate TargetType="RepeatButton">
                        <Border Height="4"
                                                    Background="{DynamicResource ControlDisabledBrush}"
                                                    CornerRadius="2"/>
                      </ControlTemplate>
                    </RepeatButton.Template>
                  </RepeatButton>
                </Track.IncreaseRepeatButton>
              </Track>

              <!-- Effective range overlay -->
              <Canvas x:Name="PART_RangeCanvas" Margin="7,0">
                <!-- Range highlight -->
                <Rectangle x:Name="rangeHighlight"
                                       Height="4" Fill="{DynamicResource AccentBrush}"
                                       Opacity="0.25" Canvas.Top="8"/>
                <!-- Min marker -->
                <Rectangle x:Name="minMarker"
                                       Width="2" Height="10"
                                       Fill="{DynamicResource AccentBrush}"
                                       Canvas.Top="5"/>
                <!-- Max marker -->
                <Rectangle x:Name="maxMarker"
                                       Width="2" Height="10"
                                       Fill="{DynamicResource AccentBrush}"
                                       Canvas.Top="5"/>
              </Canvas>
            </Grid>

            <!-- Labels row -->
            <Canvas Grid.Row="1" x:Name="PART_LabelCanvas" Margin="7,0">
              <TextBlock x:Name="minLabel"
                                   FontSize="10"
                                   Foreground="{DynamicResource AccentBrush}"/>
              <TextBlock x:Name="maxLabel"
                                   FontSize="10"
                                   Foreground="{DynamicResource AccentBrush}"/>
            </Canvas>

          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>