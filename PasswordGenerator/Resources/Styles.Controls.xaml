<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- ═══ THEME COLORS ═══ -->
  <SolidColorBrush x:Key="AccentBrush"     Color="#0B64F7"/>
  <SolidColorBrush x:Key="AmberBrush"     Color="#CA8A04"/>
  <SolidColorBrush x:Key="BorderGray"     Color="#CCCCCC"/>
  <SolidColorBrush x:Key="DisabledGray"   Color="#E0E0E0"/>
  <SolidColorBrush x:Key="BackgroundGray" Color="#F9F9F9"/>
  <SolidColorBrush x:Key="HoverGray"      Color="#F5F5F5"/>
  <SolidColorBrush x:Key="PressedGray"    Color="#E8E8E8"/>
  <SolidColorBrush x:Key="MutedText"      Color="#888888"/>

  <!-- ═══ BUTTON ═══ -->
  <Style TargetType="Button">
    <Setter Property="Padding" Value="0,5"/>
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="border"
                            BorderBrush="{StaticResource BorderGray}"
                            BorderThickness="1"
                            Background="White"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="border" Property="Background"
                                    Value="{StaticResource HoverGray}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="border" Property="Background"
                                    Value="{StaticResource PressedGray}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="border" Property="Background"
                                    Value="{StaticResource BackgroundGray}"/>
              <Setter TargetName="border" Property="BorderBrush"
                                    Value="{StaticResource DisabledGray}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ═══ CHECKBOX ═══ -->
  <Style TargetType="CheckBox">
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="CheckBox">
          <StackPanel Orientation="Horizontal">
            <Border x:Name="box"
                                Width="16" Height="16"
                                BorderBrush="{StaticResource BorderGray}"
                                BorderThickness="1"
                                CornerRadius="3"
                                Background="White"
                                VerticalAlignment="Center">
              <Path x:Name="tick"
                                  Data="M 2.5 7.7 L 5.5 10.5 L 11.5 4.5"
                                  Stroke="{StaticResource AccentBrush}"
                                  StrokeThickness="1.5"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  Visibility="Collapsed"/>
            </Border>
            <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
          </StackPanel>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="tick" Property="Visibility" Value="Visible"/>
              <Setter TargetName="box"  Property="BorderBrush"
                                    Value="{StaticResource AccentBrush}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="box" Property="BorderBrush"
                                    Value="{StaticResource AccentBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="box" Property="Background"
                                    Value="{StaticResource BackgroundGray}"/>
              <Setter TargetName="box" Property="BorderBrush"
                                    Value="{StaticResource DisabledGray}"/>
              <Setter TargetName="tick" Property="Stroke"
                                    Value="{StaticResource BorderGray}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ═══ RADIO BUTTON ═══ -->
  <Style TargetType="RadioButton">
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="RadioButton">
          <StackPanel Orientation="Horizontal">
            <Grid Width="16" Height="16" VerticalAlignment="Center">
              <Ellipse x:Name="outer"
                                     Width="16" Height="16"
                                     Stroke="{StaticResource BorderGray}"
                                     StrokeThickness="1"
                                     Fill="White"/>
              <Ellipse x:Name="inner"
                                     Width="8" Height="8"
                                     Fill="{StaticResource AccentBrush}"
                                     Visibility="Collapsed"/>
            </Grid>
            <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
          </StackPanel>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="inner" Property="Visibility" Value="Visible"/>
              <Setter TargetName="outer" Property="Stroke"
                                    Value="{StaticResource AccentBrush}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="outer" Property="Stroke"
                                    Value="{StaticResource AccentBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="outer" Property="Stroke"
                                    Value="{StaticResource DisabledGray}"/>
              <Setter TargetName="inner" Property="Fill"
                                    Value="{StaticResource BorderGray}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ═══ SLIDER ═══ -->
  <Style TargetType="Slider">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Slider">
          <Grid VerticalAlignment="Center">
            <Track x:Name="PART_Track">
              <Track.DecreaseRepeatButton>
                <RepeatButton Command="Slider.DecreaseLarge">
                  <RepeatButton.Template>
                    <ControlTemplate TargetType="RepeatButton">
                      <Border Height="4"
                                                    Background="{StaticResource AccentBrush}"
                                                    CornerRadius="2"/>
                    </ControlTemplate>
                  </RepeatButton.Template>
                </RepeatButton>
              </Track.DecreaseRepeatButton>
              <Track.Thumb>
                <Thumb>
                  <Thumb.Template>
                    <ControlTemplate TargetType="Thumb">
                      <Ellipse x:Name="knob"
                                                     Width="14" Height="14"
                                                     Fill="White"
                                                     Stroke="{StaticResource AccentBrush}"
                                                     StrokeThickness="2"/>
                      <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                          <Setter TargetName="knob" Property="Stroke"
                                                          Value="{StaticResource DisabledGray}"/>
                        </Trigger>
                      </ControlTemplate.Triggers>
                    </ControlTemplate>
                  </Thumb.Template>
                </Thumb>
              </Track.Thumb>
              <Track.IncreaseRepeatButton>
                <RepeatButton Command="Slider.IncreaseLarge">
                  <RepeatButton.Template>
                    <ControlTemplate TargetType="RepeatButton">
                      <Border Height="4"
                                                    Background="{StaticResource DisabledGray}"
                                                    CornerRadius="2"/>
                    </ControlTemplate>
                  </RepeatButton.Template>
                </RepeatButton>
              </Track.IncreaseRepeatButton>
            </Track>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- ═══ THRESHOLD SLIDER ═══ -->
  <Style x:Key="ThresholdSlider" TargetType="Slider">
    <Setter Property="IsSelectionRangeEnabled" Value="True"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Slider">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="20"/>
              <RowDefinition Height="16"/>
            </Grid.RowDefinitions>

            <!-- Track row -->
            <Grid Grid.Row="0" VerticalAlignment="Center">
              <Track x:Name="PART_Track">
                <Track.DecreaseRepeatButton>
                  <RepeatButton Command="Slider.DecreaseLarge">
                    <RepeatButton.Template>
                      <ControlTemplate TargetType="RepeatButton">
                        <Border Height="4"
                                                    Background="{StaticResource AccentBrush}"
                                                    CornerRadius="2"/>
                      </ControlTemplate>
                    </RepeatButton.Template>
                  </RepeatButton>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                  <Thumb>
                    <Thumb.Template>
                      <ControlTemplate TargetType="Thumb">
                        <Ellipse Width="14" Height="14"
                                                     Fill="White"
                                                     Stroke="{StaticResource AccentBrush}"
                                                     StrokeThickness="2"/>
                      </ControlTemplate>
                    </Thumb.Template>
                  </Thumb>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                  <RepeatButton Command="Slider.IncreaseLarge">
                    <RepeatButton.Template>
                      <ControlTemplate TargetType="RepeatButton">
                        <Border Height="4"
                                                    Background="{StaticResource DisabledGray}"
                                                    CornerRadius="2"/>
                      </ControlTemplate>
                    </RepeatButton.Template>
                  </RepeatButton>
                </Track.IncreaseRepeatButton>
              </Track>

              <!-- Effective range overlay -->
              <Canvas x:Name="PART_RangeCanvas" Margin="7,0">
                <!-- Range highlight -->
                <Rectangle x:Name="rangeHighlight"
                                       Height="4" Fill="{StaticResource AccentBrush}"
                                       Opacity="0.25" Canvas.Top="8"/>
                <!-- Min marker -->
                <Rectangle x:Name="minMarker"
                                       Width="2" Height="10"
                                       Fill="{StaticResource AccentBrush}"
                                       Canvas.Top="5"/>
                <!-- Max marker -->
                <Rectangle x:Name="maxMarker"
                                       Width="2" Height="10"
                                       Fill="{StaticResource AccentBrush}"
                                       Canvas.Top="5"/>
              </Canvas>
            </Grid>

            <!-- Labels row -->
            <Canvas Grid.Row="1" x:Name="PART_LabelCanvas" Margin="7,0">
              <TextBlock x:Name="minLabel"
                                   FontSize="10"
                                   Foreground="{StaticResource AccentBrush}"/>
              <TextBlock x:Name="maxLabel"
                                   FontSize="10"
                                   Foreground="{StaticResource AccentBrush}"/>
            </Canvas>

          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>